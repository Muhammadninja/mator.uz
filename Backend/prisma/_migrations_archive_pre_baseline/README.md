# Archived pre-baseline migrations

These migrations are archived because the project transitioned
from `db push` to a baseline migration.

Do not restore them into `prisma/migrations`.

They are kept only for historical reference until the production
baseline migration has been successfully completed.
